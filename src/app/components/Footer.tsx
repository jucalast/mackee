import React from 'react';
import styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      Criado por João Luccas &reg;
    </footer>
  );
};

export default Footer;
